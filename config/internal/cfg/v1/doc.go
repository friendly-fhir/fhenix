/*
Package cfg contains the raw config data as is parsed directly from YAML.

This is the lowest level of the config package, and is not intended to be used
directly by the user. The user should use the higher level API provided by the
config package.
*/
package cfg
