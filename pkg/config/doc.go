/*
Package config provides a mechanism for loading and consuming configuration
file formats for Fhenix projects. This package provides the computed properties
of the configuration format.

This means that defaults that are otherwise defined in the schema format are
implicitly expanded in this computed format.
*/
package config
